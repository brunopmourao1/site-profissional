# HANDOFF - site-profissional

Data do handoff: 2026-02-13
Projeto: `c:\projetos\site-profissional`

## 1) Onde estamos
- Projeto funcional (site + dashboard).
- Persistencia em `data/site.json` e uploads em `uploads/`.
- Documentacao principal: `docs/STATUS_E_BACKLOG_2026-02-13.md`.

## 2) Ajustes mais recentes (importante)
- "Fundo dos quadros internos" por secao:
  - campos: `frame_bg_mode` e `frame_bg_color`.
  - aplicado no frontend via CSS vars `--frame-bg` e `--subframe-bg`.
- Tipografia/cor por secao:
  - `text_color` + `text_font/text_size` aplicados no container da secao.
  - `title_font` aplicado em titulos de cards e no titulo do formulario.
- Carousel por secao:
  - 2 a 5 midias (imagem/video), link por slide e layout `boxed`/`full`.
- Google Maps:
  - `map_embed_url` normaliza e rejeita links nao embutiveis (ex: `maps.app.goo.gl`).

## 3) O que testar na proxima sessao
- Rodar `docs/PLANO_DE_TESTES.md` completo.
- Dar atencao especial em:
  - funcoes 6 (galeria) e 10 (carousel)
  - mapas (colando iframe src correto)
  - secao com fundo escuro + `frame_bg_mode=section` + `text_color` claro
  - mobile (menu + cards + galerias + carousel)

## 4) Comandos rapidos
Subir servidor:
```powershell
cd C:\projetos\site-profissional
php -S localhost:8000
```

Lint:
```powershell
php -l index.php
php -l admin\index.php
php -l includes\site_data.php
```

